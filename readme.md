# Spotify Export Cleaner

Interactive utility to cleanup Spotify streaming history before importing into Stats.fm

## Usage

1. Install poetry dependencies
2. Add your exported JSON files to `/exports`
3. Run `python3 main.py`
4. Upload cleaned files in `/cleaned` to Stats.fm
